This directory holds test data for unit tests. These are marshalled to the binary directory on build.

*Note:* Only to be used for very small data sets. Larger data sets should not be in Git this way.
