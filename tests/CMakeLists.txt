enable_testing()
if(NOT OHM_GPU_API STREQUAL "CUDA")
  add_subdirectory(data)
  add_subdirectory(gputiltest)
  add_subdirectory(ohmtest)
else(NOT OHM_GPU_API STREQUAL "CUDA")
  add_subdirectory(gputiltest)
endif(NOT OHM_GPU_API STREQUAL "CUDA")
